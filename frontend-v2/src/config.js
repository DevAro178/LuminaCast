// API Configuration
// You can change the baseURL here if your server IP changes

const config = {
  // Replace with your local IP if accessing from another device (e.g., 'http://192.168.1.5:8000')
  apiBaseUrl: 'http://13.127.7.196:8000',
};

export default config;
